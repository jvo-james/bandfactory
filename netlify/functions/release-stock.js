const admin=require('firebase-admin');const crypto=require('crypto');const {restoreDeductions,defaultCatalog}=require('./order-stock');if(!admin.apps.length){admin.initializeApp({credential:admin.credential.cert({projectId:process.env.FIREBASE_PROJECT_ID,clientEmail:process.env.FIREBASE_CLIENT_EMAIL,privateKey:String(process.env.FIREBASE_PRIVATE_KEY||'').replace(/\\n/g,'\n')})});}const db=admin.firestore(),hash=value=>crypto.createHash('sha256').update(String(value||'')).digest('hex'),headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type','Access-Control-Allow-Methods':'POST, OPTIONS'};exports.handler=async event=>{if(event.httpMethod==='OPTIONS')return {statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return {statusCode:405,headers,body:JSON.stringify({ok:false,error:'POST required'})};try{const body=JSON.parse(event.body||'{}'),orderId=String(body.orderId||'').trim(),token=String(body.token||'');if(!orderId||!token)return {statusCode:400,headers,body:JSON.stringify({ok:false})};const tokenRef=db.collection('checkoutTokens').doc(orderId),orderRef=db.collection('orders').doc(orderId),reservationRef=db.collection('stockReservations').doc(orderId),smoothRef=db.doc('products/smooth'),apparelRef=db.doc('products/spandexTubeTop'),catalogRef=db.doc('products/catalog');await db.runTransaction(async tx=>{const [tokenSnap,orderSnap,reservationSnap]=await Promise.all([tx.get(tokenRef),tx.get(orderRef),tx.get(reservationRef)]);if(!tokenSnap.exists||tokenSnap.data()?.tokenHash!==hash(token))throw new Error('Invalid checkout session');if(!reservationSnap.exists||reservationSnap.data()?.status!=='reserved')return;if(orderSnap.exists&&orderSnap.data()?.payment==='Paid'){tx.set(reservationRef,{status:'finalized',finalizedAt:admin.firestore.FieldValue.serverTimestamp()},{merge:true});return;}const [smoothSnap,apparelSnap,catalogSnap]=await Promise.all([tx.get(smoothRef),tx.get(apparelRef),tx.get(catalogRef)]),restored=restoreDeductions(smoothSnap.exists?smoothSnap.data():{},apparelSnap.exists?apparelSnap.data():{},catalogSnap.exists?catalogSnap.data():defaultCatalog(),reservationSnap.data()?.deducted||{}),t=admin.firestore.FieldValue.serverTimestamp();if(smoothSnap.exists)tx.set(smoothRef,{styles:restored.styles,colors:restored.styles.flat?.colors||{},updatedAt:t},{merge:true});if(apparelSnap.exists)tx.set(apparelRef,{sizes:restored.sizes,updatedAt:t},{merge:true});if(catalogSnap.exists||Object.keys(reservationSnap.data()?.deducted?.catalog||{}).length)tx.set(catalogRef,{...restored.catalog,updatedAt:t},{merge:false});tx.set(reservationRef,{status:'released',releasedAt:t},{merge:true});if(orderSnap.exists)tx.set(orderRef,{stockSyncStatus:'released'},{merge:true});});return {statusCode:200,headers,body:JSON.stringify({ok:true})};}catch(error){console.error('[Band Factory] release-stock',error);return {statusCode:500,headers,body:JSON.stringify({ok:false})};}};